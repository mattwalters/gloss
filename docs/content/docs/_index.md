---
title: "Documentation"
# Pinned because [permalinks.section] derives :slug from the title, which
# would publish this index at /documentation/ while the header nav, the
# child pages and every inbound link say /docs/.
slug: "docs"
---

Writ stores code review and issue operations inside the git repository itself.

The full flag-by-flag surface is the [CLI Reference](../cli/), generated from
the command table in `cmd/writ`.
