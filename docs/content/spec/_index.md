---
title: "Specification"
# Pinned because [permalinks.section] derives :slug from the title, which
# would publish this index at /specification/ while the header's Spec link
# and every child page say /spec/.
slug: "spec"
---

The Writ specification defines the data model, operation vocabulary, and conformance rules for interoperable implementations.

The conformance fixtures are the ground truth — when prose and fixtures disagree, the fixtures win and the prose gets fixed.
