{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://writ.dev/spec/identifiers.schema.json",
  "title": "Writ identifiers, references, and repo registry (v1)",
  "description": "Workspace-global object identifiers, repo designators, references, and workspace repo registry entries. Normative text: spec/identifiers.md. Cross-field invariants the schema cannot express (resolution algorithm, registry uniqueness, opaque-fallback handling) are defined in the spec text and enforced by validators alongside this schema. additionalProperties is deliberately never false: unknown fields are preserved and ignored.",
  "type": "object",
  "properties": {
    "reference": { "$ref": "#/$defs/reference" },
    "context": { "$ref": "#/$defs/resolution-context" },
    "registry": {
      "type": "array",
      "items": { "$ref": "#/$defs/repo-entry" }
    },
    "expected": { "$ref": "#/$defs/resolution-expected" }
  },
  "required": ["reference"],
  "$defs": {
    "person-id": {
      "type": "string",
      "minLength": 1,
      "description": "Person identifier: normalized collaborative actor identity (email address, lowercase, trimmed whitespace) per spec/identifiers.md."
    },
    "object-id": {
      "type": "string",
      "pattern": "^[0-9a-f]{32}$",
      "description": "Canonical Writ-minted object ID: 128 bits of entropy as 32 lowercase hexadecimal characters."
    },
    "repo-id": {
      "type": "string",
      "pattern": "^[0-9a-f]{32}$",
      "description": "Writ repository designator: 32 lowercase hexadecimal characters minted at writ init."
    },
    "reference": {
      "type": "string",
      "minLength": 1,
      "maxLength": 513,
      "pattern": "^([0-9a-f]{32}#)?[^#\\s]+$",
      "description": "Reference string: optional 32-hex repo-id followed by #, followed by an object-id (canonical 32-hex or envelope-legal opaque ASCII without #)."
    },
    "canonical-reference": {
      "type": "string",
      "pattern": "^([0-9a-f]{32}#)?[0-9a-f]{32}$",
      "description": "Canonical reference: bare <object-id> (32 hex) or <repo-id>#<object-id> (32 hex # 32 hex)."
    },
    "repo-slug": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[^\\s]+$",
      "description": "Repository slug in the registry: non-empty string without whitespace."
    },
    "remote-url": {
      "type": "string",
      "minLength": 1,
      "description": "Remote URL string for discovery: non-empty string."
    },
    "repo-entry": {
      "type": "object",
      "properties": {
        "repo_id": { "$ref": "#/$defs/repo-id" },
        "slug": { "$ref": "#/$defs/repo-slug" },
        "remotes": {
          "type": "array",
          "items": { "$ref": "#/$defs/remote-url" }
        },
        "is_workspace": {
          "type": "boolean"
        }
      },
      "required": ["repo_id", "slug"]
    },
    "resolution-context": {
      "type": "object",
      "properties": {
        "local_repo_id": { "$ref": "#/$defs/repo-id" }
      }
    },
    "resolution-expected": {
      "type": "object",
      "properties": {
        "resolved": { "type": "boolean" },
        "scope": {
          "type": "string",
          "enum": ["local", "cross-repo", "unresolved"]
        },
        "repo_id": { "$ref": "#/$defs/repo-id" },
        "object_id": { "type": "string", "minLength": 1 }
      },
      "required": ["resolved", "scope", "object_id"]
    }
  }
}
