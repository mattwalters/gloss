# spec

The convention doc, JSON schemas, and conformance fixtures — the real
standard, per ARCHITECTURE.md ("Spec = fixtures"). Not yet written; see
../VISION.md and ../ARCHITECTURE.md for the shape this is expected to take.
